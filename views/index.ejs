<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Create a user</title>
</head>
<body>
  <ul class="quotes">
      <% for(var i=0; i<quotes.length;i++) {%>
        <li class="quote">
          <span><%= quotes[i].name %></span>
          <span><%= quotes[i].quote %></span>
        </li>
      <% } %>
    </ul>

    <form action="/quotes" method="POST">
      <input type="text" placeholder="Name" name="name">
      <input type="text" placeholder="Note" name="quote">
      <button type="submit">Submit</button>
    </form>
    <div>
      <h2>Replace the last post by Nouzhan with a post by Sepehr.</h2>
      <button id="update"> Sepehr-ify! </button>
      <h2>Delete the first post by Sepehr</h2>
      <button id="delete"> Delete Sepehr's first post</button>
      <script src="main.js"></script>
    </div>
</body>
</html>
